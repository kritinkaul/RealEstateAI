# Local: optional if you call the API directly (dev usually uses Vite /api proxy).
VITE_API_BASE_URL=http://127.0.0.1:8001

# Production (Vercel): REQUIRED — your Render (or other) API origin, no trailing slash.
# VITE_API_BASE_URL=https://your-service.onrender.com
